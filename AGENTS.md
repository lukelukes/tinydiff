## Description

A Tauri/React desktop application to view Git diffs in an aesthetic way and perform GitHub style code reviews.

## Constraints

- Do not leave comments, ever, for any reason
- We use `just` and `bun` to run tasks
  - All you need is: `just cargo::<check|build>` and `bun <validate>`
